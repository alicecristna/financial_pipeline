## Day 9 复盘笔记

### 核心突破
1. `shift()` 是因子计算的核心，动量 = close / close.shift(20) - 1
2. `np.where` 和 `np.select` 替代了所有 if-else，速度飞跃
3. 策略收益 = 信号.shift(1) * 实际收益（信号滞后一天是关键）

### 踩坑记录
- 忘记 `shift(1)` 导致策略收益包含了未来信息（回测大忌！）
- `np.select` 的 `default` 参数很重要，否则未匹配的会变成 0（恰好也是我们想要的空仓）

### 速度对比
- 向量化因子计算: _____ ms（1000 行）
- iterrows 方法: _____ ms
- 速度差异: _____ 倍

### 明日重点
- 多个因子如何组合？
- 如何避免回测中的常见陷阱（未来函数、幸存者偏差）？